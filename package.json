{
  "name": "cachefinance",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-cachefinance.mjs",
    "build:check": "node scripts/build-cachefinance.mjs --check",
    "validate": "npm run build:check && npm run test:coverage",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.7",
    "vitest": "^3.2.4"
  }
}
